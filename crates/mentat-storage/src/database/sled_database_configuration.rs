pub fn todo() {
    todo!(
        "this is another one of their weird alternative builder patterns
        where they use a bunch of function pointers to set values.
        do this in a normal builder pattern instead"
    )
}
